;; -*-lisp-*-
;;
;; Author: Andy Nagels
;; Date: 2018-04-14
;; StumWM configuration file.

(in-package :stumpwm)
(setf *default-package* :stumpwm)

;; Startup message
(setf *startup-message* nil)

;; Run a shell command
;(run-shell-command "myscript")

;; Load Mod4+ as modifier
; TODO

;; Load modules
(load "~/.config/stumpwm/commands.lisp")
(load "~/.config/stumpwm/placement.lisp")
(load "~/.config/stumpwm/keybindings.lisp")
(load "~/.config/stumpwm/theme.lisp")
(load "~/.config/stumpwm/utilities.lisp")
(load "~/.config/stumpwm/modeline.lisp")
(when "initializing"
  (mode-line))
(load-module "end-session") ; Gracefully end programs when ending user session
(load-module "globalwindows") ; Navigate between windows from all workspaces
(load-module "urgentwindows") ; Get urgent windows

(setf *startup-message* "System ready.")
